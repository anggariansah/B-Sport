<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.angga.b_sport">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginMenu" />
        <activity android:name=".Register">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LoginMenu" />
        </activity>
        <activity android:name=".LoginUser">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LoginMenu" />
        </activity>
        <activity android:name=".LoginOwner">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LoginMenu" />
        </activity>
        <activity android:name=".PilihBooking">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Pilih_lapangan_8" />
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".MenuUtamaUser" />
        <activity android:name=".MenuUtamaOwner" />
        <activity android:name=".WaktuMain">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Menu_Booking_11" />
        </activity>
        <activity android:name=".UbahDeskripsi">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuUtamaOwner" />
        </activity>
        <activity android:name=".DetailPlace">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuUtamaUser" />
        </activity>
        <activity android:name=".Pilih_lapangan_8">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DetailPlace" />
        </activity>
        <activity android:name=".Pemesanan_10">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".PilihBooking" />
        </activity>
        <activity android:name=".Menu_Booking_11" />
        <activity android:name=".Notifikasi_Owner_19">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuUtamaOwner" />
        </activity>
        <activity android:name=".Kelola_Lapangan_Owner_16">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuUtamaOwner" />
        </activity>
        <activity android:name=".TambahTempat"></activity>
    </application>

</manifest>